<h1>All Listings</h1>
<% if @listings.any? %>
    <%# listing.photo %>
    <% @listings.each do |listing| %>
    <div class="listing">
    <%= link_to listing.name, listing_path(listing) %>
  </div>
    <% end %>
<% end %>
